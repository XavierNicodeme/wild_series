{% extends 'base.html.twig' %}

{% block body %}
    <h1>Saison {{ season.number }} de {{ program.title }}</h1>
    <h2>Description : {{ season.description }}</h2>
    <h5>Liste des épisodes </h5>
    <ul>
        {% for episode in season.episodes %}
            <li>
                <a href="{{ path('program_episode_show', {slug: program.slug, season: season.id, episode: episode.id}) }}">
                    Episode {{ episode.number }} - {{ episode.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
    <a href="{{ path("program_show", { slug: program.slug}) }}">Retour</a>
{% endblock %}